#include <functional>
#include <queue>
#include <vector>
#include <iostream>

using namespace std;
 
int main() {
	int N;
	cin >> N;
	auto func = [&](bool arr[N][N]) {

	};
	int arr[N][N];
	func(arr);
	return 0; 
}
