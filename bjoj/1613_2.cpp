#include <iostream>

using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    while (k--) {
        int a, b;
        cin >> a >> b;
        a--, b--;
            }
    vector<int> p;

}
